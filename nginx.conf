events {}
http {
    upstream backend {
        server 150.136.161.131;
        server 129.153.1.233;
        server bilanx:8080;
    }

    server {
        location / {
            proxy_pass http://backend;
        }
    }
}